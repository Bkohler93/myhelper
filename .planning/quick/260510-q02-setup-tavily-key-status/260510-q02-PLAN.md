---
quick_id: 260510-q02
slug: setup-tavily-key-status
description: Update setup command to show existing Tavily API key status and allow skipping re-entry
date: 2026-05-10
must_haves:
  truths:
    - wizard.go Stage 4 reads existing config and detects pre-existing tavily_key
    - when key exists, wizard prints info line showing key is configured (masked)
    - prompt text changes to indicate enter keeps existing key
    - pressing enter with existing key keeps the existing key (no overwrite)
    - entering a new key replaces the existing key
    - no existing key: behavior unchanged from current
  artifacts:
    - internal/wizard/wizard.go (Stage 4 updated)
    - internal/wizard/wizard_test.go (new tests added)
---

# Quick Task 260510-q02: Update setup Tavily key UX

## Task 1: Add readHomeConfig helper + update Stage 4 in wizard.go

**Files:** `internal/wizard/wizard.go`

**Action:**
1. Add `readHomeConfig() map[string]interface{}` helper (reads current config, returns empty map on error)
2. In Stage 4, before the prompt: call `readHomeConfig()`, check for `tavily_key`
3. If existing key found: print info line with masked key; change prompt to say "press enter to keep existing"
4. If user presses Enter AND existing key: skip saving (keep as-is)
5. If user types new key: save it (same as now)

**Verify:** `go build ./...` passes

**Done:** Stage 4 UX updated

---

## Task 2: Add tests for new behavior

**Files:** `internal/wizard/wizard_test.go`

**Action:**
1. `TestRun_TavilyKeyExisting_SkipKeepsExisting` — pre-write config with tavily_key, run wizard, press enter at Tavily prompt, verify original key unchanged in config
2. `TestRun_TavilyKeyExisting_NewKeyReplaces` — pre-write config with tavily_key, run wizard, enter new key at Tavily prompt, verify new key in config
3. Verify existing tests still pass

**Verify:** `go test ./internal/wizard/...` passes

**Done:** All tests passing
