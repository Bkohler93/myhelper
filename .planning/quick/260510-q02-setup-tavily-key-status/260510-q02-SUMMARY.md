---
quick_id: 260510-q02
slug: setup-tavily-key-status
status: complete
date: 2026-05-10
---

# Quick Task 260510-q02: Show existing Tavily API key status in setup

## What was done

Updated `internal/wizard/wizard.go` Stage 4 (Tavily API key prompt) to detect when a key is already configured and provide context-aware UX.

### Changes

**`internal/wizard/wizard.go`**
- Added `readHomeConfig()` helper — reads current home config into a map (graceful on error)
- Added `maskKey(key string) string` helper — shows first 4 + last 4 chars, bullets in between; all-bullets for keys ≤8 chars
- Stage 4: reads existing config before prompting; if `tavily_key` is set, prints info line with masked key and changes prompt to "press enter to keep existing"; if user presses Enter, prints "Keeping existing Tavily key." and skips save; if user types new key, saves it (same as before); if no existing key, behavior is unchanged

**`internal/wizard/wizard_test.go`**
- `TestRun_TavilyKeyExisting_SkipKeepsExisting` — verifies pressing Enter preserves existing key
- `TestRun_TavilyKeyExisting_NewKeyReplaces` — verifies entering a new key replaces the existing one
- `TestMaskKey` — verifies masking behavior for short, medium, and long keys

## Test results

All 18 wizard tests pass.
