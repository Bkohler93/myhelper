---
quick_id: 260510-q01
slug: model-flag-setup-rework
status: complete
date: 2026-05-10
commit: 95f0116
---

# Quick Task 260510-q01: Summary

## What was done

**Task 1 — `--model` flag** (`cmd/root.go`):
- Added `modelFlag string` package var and registered `--model` as a persistent string flag on `rootCmd`
- `ApplyFlagOverrides` now sets `cfg.Model = modelFlag` when the flag is non-empty, giving per-invocation model override across all commands

**Task 2 — Setup wizard model selection** (`internal/wizard/wizard.go`):
- Added `listModels(endpoint string) ([]string, error)` — calls Ollama `/api/tags`, returns sorted model names
- Replaced the hardware-detect + auto-recommend + "Pull X now?" flow with: display numbered list of available models → user picks, OR enter `0` to pull a new model by name
- `detectMemoryMiB()` and `recommendModel()` remain in the file (not deleted) but are no longer called from `Run()`
- Updated wizard docstring to reflect the new 5-stage flow

**Test fixes** (`internal/wizard/wizard_test.go`):
- Updated `TestRun_SkipModel_EmptyThenProvided` and `TestRun_SkipModel_EmptyTwice` — removed stale `n` input that was for the old "Pull X now?" prompt, which no longer exists; updated inline comments

All tests pass: `go test ./...`
