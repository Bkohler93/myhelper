# Requirements Archive — v3.2 Observability & Polish

**Archived:** 2026-04-24
**Status:** ✅ All requirements satisfied

---

## v3.2 Requirements (Final)

### INSP — inspect command

- [x] **INSP-01**: User can run `myhelper inspect <query>` to see per-stage retrieval diagnostics without sending a model response — **VALIDATED v3.2**
- [x] **INSP-02**: `inspect` output shows relevance gate decision (pass / fail + raw LLM answer) — **VALIDATED v3.2**
- [x] **INSP-03**: `inspect` output shows how many symbols passed the pre-filter stage and lists them with scores — **VALIDATED v3.2**
- [x] **INSP-04**: `inspect` output shows which symbols survived LLM re-ranking vs which were dropped — **VALIDATED v3.2**
- [x] **INSP-05**: `inspect` respects `--no-context` flag (skips all retrieval stages, shows that context was bypassed) — **VALIDATED v3.2**

### UX — loading spinners

- [x] **UX-01**: A spinner is shown while SearXNG fetches results; disappears when fetch completes — **VALIDATED v3.2**
- [x] **UX-02**: A spinner is shown while the LLM search-gate call runs; disappears when decision is made — **VALIDATED v3.2**
- [x] **UX-03**: A spinner is shown while the LLM re-rank call runs; disappears when re-ranking completes — **VALIDATED v3.2**

### BUG — correctness fixes

- [x] **BUG-01**: SearXNG URL construction tolerates a trailing slash on the configured endpoint (no double-slash in path) — **VALIDATED v3.2**
- [x] **BUG-02**: `BuildContext` and `BuildInspectContext` surface the `llmReRank` error instead of silently discarding it — **VALIDATED v3.2**

### CLN — dead code removal

- [x] **CLN-01**: `cmd/search.go:countTokens` removed; its callers redirected to the shared `retrieval` package helper — **VALIDATED v3.2**
- [x] **CLN-02**: `PackageEntry.Responsibility` removed from the re-rank pass; no unused field silently ignored — **VALIDATED v3.2**
- [x] **CLN-03**: `Symbol.CallEdges` and `Symbol.TypeRefs` documented as reserved-for-future-use in code — **VALIDATED v3.2**

### CTX — context injection fix

- [x] **CTX-03**: Dual context injection fixed — `LoadContext` is defined but never called; no dual injection occurs — **VALIDATED v3.2** (closed without code change)

### PERF — performance

- [x] **PERF-01**: `microPassFile` uses the stored `Symbol.Start` / `Symbol.End` line numbers from the artifact instead of re-parsing AST via `ExtractSymbolMap` at runtime — **VALIDATED v3.2**

---

## Requirement Outcomes

| REQ-ID | Outcome | Notes |
|--------|---------|-------|
| INSP-01–05 | Shipped as specified | inspect command fully wired; human smoke tests deferred (live Ollama needed) |
| UX-01–03 | Shipped as specified | Goroutine spinners using stdlib; live TTY test deferred |
| BUG-01 | Fixed | `strings.TrimRight` in `internal/search/search.go:98` |
| BUG-02 | Fixed | Named `reRankErr` at both call sites in `retrieval.go` |
| CLN-01 | Fixed | `countTokens` deleted; callers use `history.New(...).TokenCount()` |
| CLN-02 | Fixed | `pkgs` param removed from `llmReRank`; not wired in alternative |
| CLN-03 | Fixed | Struct comments updated; function doc comment inconsistency remains (doc only) |
| CTX-03 | Closed | `LoadContext` never called — no injection path exists; no code change needed |
| PERF-01 | Fixed | `microPassFile` uses stored symbols; `ExtractSymbolMap` fallback for unindexed files |

---

## Traceability

| Phase | Requirements | Status |
|-------|-------------|--------|
| 21 — inspect Command | INSP-01, INSP-02, INSP-03, INSP-04, INSP-05 | All satisfied |
| 22 — Search Pipeline Spinners | UX-01, UX-02, UX-03 | All satisfied |
| 23 — Cleanup & Correctness | BUG-01, BUG-02, CLN-01, CLN-02, CLN-03, CTX-03, PERF-01 | All satisfied |

---

## Future Requirements

- `Symbol.CallEdges` / `Symbol.TypeRefs` actively consumed by retrieval pipeline for call-graph-aware ranking (SYM-05, SYM-06) — deferred until retrieval quality warrants the complexity
- Phase 11 VERIFICATION.md — formal verification of RET-01–06 — deferred (covered by downstream phases and 13 unit tests)
